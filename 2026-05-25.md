# 2026-05-25 教学记录

共 3 讲

---

## 09:01 | 中间件

### Redis 数据结构与缓存实战

📚 [中间件] Redis 数据结构与缓存实战

>> 核心要点

1. 要点一：五种核心数据结构——String（计数器/分布式锁）、Hash（对象缓存）、List（消息队列）、Set（去重/标签）、Sorted Set（排行榜/延迟队列）
2. 要点二：缓存三大模式——Cache Aside（旁路缓存，最常用）、Read/Write Through（缓存代理）、Write Behind（异步写入）
3. 要点三：RTD 场景应用——用 Redis 缓存设备状态、Pub/Sub 实现机台告警推送、Sorted Set 做 Lot 优先级队列

>> 一句话总结
Redis 不是缓存数据库，而是数据结构服务器——用对数据结构，事半功倍。

>> 今日一题
Q: 在 Cache Aside 模式中，正确的更新策略是？
A) 先更新缓存，再更新数据库  B) 先更新数据库，再删除缓存  C) 先删除缓存，再更新数据库  D) 同时更新缓存和数据库

---

## 10:00 | CI/CD

### CI/CD 流水线实战

[CI/CD] CI/CD Pipeline

上期答案: B
Cache Aside: 先更新DB，再删除缓存

核心要点:
1. Pipeline as Code: Jenkinsfile/GitLab CI YAML 定 Build/Test/Deploy
2. 声明式(pipeline{}) vs 脚本式(node{})
3. RTD: git push -> 自动测试 -> 灰度发布 -> 健康检查

一句话: CI/CD用代码定义交付流程

今日一题: Jenkins声明式流水线包裹在?
A) node{} B) stage{} C) pipeline{} D) steps{}

---

## 11:04 | Shell

### Shell 脚本错误处理与 trap 机制

上期答案: C
Jenkins声明式流水线: pipeline { agent any; stages { stage('Build') { steps {...} } } }
脚本式用 node {}，声明式用 pipeline {}

📚 [Shell] Shell 脚本错误处理与 trap 机制

>> 核心要点

1. 要点一：set -e 遇错即停 + set -o pipefail 管道错误也停——生产脚本第一道防线，避免带着错误继续执行导致数据损坏
2. 要点二：trap 'cleanup' EXIT——无论成功失败都执行清理，RTD 脚本中用于删临时文件、释放文件锁、回滚操作
3. 要点三：cmd || { echo "FAIL"; exit 1; } 精细错误处理——比 set -e 更灵活，适合需降级而非直接退出的场景

>> 一句话总结
Shell 脚本健壮性靠三道防线：set -e（全局）+ trap EXIT（兜底）+ &&/|| 显式检查（精细化）。

>> 今日一题
Q: 以下哪种写法确保脚本退出时一定执行 cleanup？
A) cleanup && exit  B) trap cleanup EXIT  C) trap cleanup ERR  D) exit && cleanup

---

